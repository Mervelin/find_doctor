<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title || 'Find Doctor' %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id="header">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <%= link_to('Find Doctor', root_path, class: 'navbar-brand logo') %>
          </div>
          <ul class="nav nav-pills navbar-right">
            <li role="presentation" class="<%= 'active' if @page_title == 'Panel Pacjenta' %>">
              <%= link_to('Panel Pacjenta', dashboard_patients_path, class: 'navbar-brand')
              %></a></li>
            <li role="presentation" class="<%= 'active' if @page_title == 'Panel Lekarza' %>">
              <%= link_to('Panel Lekarza', dashboard_doctors_path, class: 'navbar-brand') %></li>
          </ul>
          <% if signed_in? %>
              <p class="navbar-text navbar-right" style="margin-right: 40px;">Zalogowany jako
                <% if patient_signed_in? then %>
                    pacjent <%= current_patient.email %>
                <% else %>
                    lekarz <%= current_doctor.email %>
                <% end %>
              </p></div>
          <% end %>
        </div>
      </nav>
    </div>
    <div id="container">

      <% if notice %>
        <p class="notice"><%= notice %></p>
      <% end %>

      <% if alert %>
        <p class="alert"><%= alert %></p>
      <% end %>

      <div id="content">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
